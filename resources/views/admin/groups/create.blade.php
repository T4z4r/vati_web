@extends('layouts.admin')
@section('title','Create Group')
@section('content')
<div class="page-head"><div><p class="eyebrow">GROUP LENDING</p><h1>Create a member group</h1><p>Assign an operating branch, meeting schedule, and loan officer.</p></div><a class="btn btn-secondary" href="{{ route('admin.groups.index') }}">← Back</a></div>
<form class="card" method="POST" action="{{ route('admin.groups.store') }}">@csrf<div class="card-body"><div class="form-grid"><label>Branch<select name="branch_id" required><option value="">Select branch</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(old('branch_id')==$branch->id)>{{ $branch->branch_name }}</option>@endforeach</select></label><label>Group name<input name="group_name" value="{{ old('group_name') }}" required></label><label>Loan officer<select name="loan_officer_id"><option value="">Unassigned</option>@foreach($officers as $officer)<option value="{{ $officer->id }}">{{ $officer->name }}</option>@endforeach</select></label><label>Meeting day<select name="meeting_day"><option value="">Select day</option>@foreach(['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'] as $day)<option>{{ $day }}</option>@endforeach</select></label><label>Meeting time<input type="time" name="meeting_time"></label><label>Region<input name="region"></label><label>District<input name="district"></label><label>Ward<input name="ward"></label><label>Meeting location<input name="location"></label></div><p class="muted">The group code is generated automatically when saved.</p><div class="form-actions"><a class="btn btn-secondary" href="{{ route('admin.groups.index') }}">Cancel</a><button class="btn btn-primary">Create group</button></div></div></form>
@endsection
