@extends('layouts.admin')
@section('title','Reports')
@section('content')
<div class="page-head"><div><p class="eyebrow">{{ __('MANAGEMENT INFORMATION') }}</p><h1>{{ __('Portfolio reports') }}</h1><p>{{ __('Monitor origination, collections, arrears, and portfolio quality.') }}</p></div><button class="btn btn-secondary" onclick="window.print()">{{ __('Print report') }}</button></div>
<form class="card card-body filters"><select name="branch_id"><option value="">{{ __('All branches') }}</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected($branchId==$branch->id)>{{ $branch->branch_name }}</option>@endforeach</select><select name="group_id"><option value="">{{ __('All groups') }}</option>@foreach($groups as $group)<option value="{{ $group->id }}" @selected(request('group_id')==$group->id)>{{ $group->group_name }}</option>@endforeach</select><select name="loan_product_id"><option value="">{{ __('All products') }}</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(request('loan_product_id')==$product->id)>{{ $product->name }}</option>@endforeach</select><input type="date" name="date_from" value="{{ $from->format('Y-m-d') }}"><input type="date" name="date_to" value="{{ $to->format('Y-m-d') }}"><button class="btn btn-primary">{{ __('Run report') }}</button></form><br>
<div class="stats"><div class="stat"><small>{{ __('Active members') }}</small><strong>{{ number_format($activeMembers) }}</strong></div><div class="stat"><small>{{ __('Applications in period') }}</small><strong>{{ number_format($applications) }}</strong></div><div class="stat gold"><small>{{ __('Disbursements') }}</small><strong>TZS {{ number_format($disbursements) }}</strong></div><div class="stat"><small>{{ __('Collections') }}</small><strong>TZS {{ number_format($collections) }}</strong></div><div class="stat gold"><small>{{ __('Portfolio outstanding') }}</small><strong>TZS {{ number_format($portfolio) }}</strong></div><div class="stat"><small>{{ __('PAR 1') }}</small><strong>{{ number_format($par1,2) }}%</strong></div><div class="stat"><small>{{ __('PAR 7') }}</small><strong>{{ number_format($par7,2) }}%</strong></div><div class="stat"><small>{{ __('PAR 30') }}</small><strong>{{ number_format($par30,2) }}%</strong></div></div>
<div class="card"><div class="card-head"><h2>{{ __('Loans in arrears') }}</h2><span class="muted">{{ __('Top 20 by outstanding balance') }}</span></div><div class="table-wrap"><table><thead><tr><th>{{ __('Loan') }}</th><th>{{ __('Member') }}</th><th>{{ __('Group') }}</th><th>{{ __('Outstanding') }}</th><th>{{ __('Maturity') }}</th><th>{{ __('Status') }}</th></tr></thead><tbody>@forelse($arrearsLoans as $loan)<tr><td><a class="table-link" href="{{ route('admin.loans.show',$loan) }}">{{ $loan->loan_number }}</a></td><td>{{ $loan->member->first_name }} {{ $loan->member->last_name }}</td><td>{{ $loan->group->group_name }}</td><td class="money">TZS {{ number_format($loan->total_balance) }}</td><td>{{ $loan->maturity_date?->format('d M Y')??'—' }}</td><td><span class="badge {{ $loan->status->value }}">{{ $loan->status->value }}</span></td></tr>@empty<tr><td colspan="6" class="empty">{{ __('No arrears in the selected portfolio.') }}</td></tr>@endforelse</tbody></table></div></div>
@endsection
